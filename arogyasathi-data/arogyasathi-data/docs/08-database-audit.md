# Database Audit

## Current State
`NOT_IMPLEMENTED`

Data is currently stored in local JSON/Markdown files.

## Future Requirements
- Move schemes, rules, benefits, translations, and documents into a relational database (e.g., PostgreSQL).
- Ensure citizen data is either ephemeral (session only) or securely encrypted, strictly minimizing PII.
- Setup a vector database for RAG operations.
